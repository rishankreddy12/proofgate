apiVersion: v1
kind: Service
metadata:
  name: {{ include "proofgate.fullname" . }}
  labels:
    {{- include "proofgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.adminPort }}
      targetPort: admin
      protocol: TCP
      name: admin
  selector:
    {{- include "proofgate.selectorLabels" . | nindent 4 }}
